---
- name: Initialize update summary for this host
  ansible.builtin.set_fact:
    podman_update_summary: []

- name: Update each Podman Quadlet unit
  ansible.builtin.include_tasks: update_unit.yml
  loop: "{{ podman_units }}"
  loop_control:
    loop_var: unit
    label: "{{ unit.name }}"
