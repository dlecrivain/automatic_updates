---
- name: Remove ansible_container snapshots across all container-updated VMs
  hosts: "{{ target_hosts | default('proxmox_all_qemu:!lpkat101:!semaphore101') }}"
  gather_facts: false
  tasks:
    - name: Check existing snapshots for this VM
      ansible.builtin.command: >
        pvesh get /nodes/{{ proxmox_node }}/qemu/{{ proxmox_vmid }}/snapshot
      delegate_to: "{{ proxmox_api_host }}"
      remote_user: "{{ proxmox_ssh_user }}"
      vars:
        ansible_python_interpreter: "{{ proxmox_python_interpreter }}"
      register: snapshot_list
      changed_when: false

    - name: Remove the ansible_container snapshot if it exists
      ansible.builtin.command: >
        pvesh delete /nodes/{{ proxmox_node }}/qemu/{{ proxmox_vmid }}/snapshot/ansible_container
      delegate_to: "{{ proxmox_api_host }}"
      remote_user: "{{ proxmox_ssh_user }}"
      vars:
        ansible_python_interpreter: "{{ proxmox_python_interpreter }}"
      when: "'ansible_container' in snapshot_list.stdout"
